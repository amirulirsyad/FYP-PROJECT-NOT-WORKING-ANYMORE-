<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Application title -->
    <TextView
        android:id="@+id/appTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Indoor Positioning"
        android:textSize="24sp"
        android:textStyle="bold"
        android:layout_gravity="center_horizontal"
        android:layout_marginBottom="24dp" />

    <!-- "Connect to WiFi" button -->
    <Button
        android:id="@+id/connectToWifiButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Connect to WiFi"
        android:textSize="18sp"
        android:layout_marginBottom="16dp" />

    <!-- "Save Location" button -->
    <Button
        android:id="@+id/wayfindingButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Wayfinding"
        android:textSize="18sp"
        android:layout_marginBottom="16dp" />

    <!-- Add some spacing -->
    <Space
        android:layout_width="match_parent"
        android:layout_height="16dp" />

    <!-- Add an image or a descriptive text to enhance the user experience -->
    <ImageView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:src="@drawable/indoor_map_image"
        android:scaleType="fitCenter"
        android:layout_marginBottom="24dp" />

    <!-- Add a description or guidance for the user -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Explore indoor locations and get directions with ease!"
        android:textSize="16sp"
        android:textColor="#666666"
        android:layout_gravity="center_horizontal" />

</LinearLayout>
